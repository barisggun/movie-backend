@model ResetPasswordViewModel

@{
    ViewData["Title"] = "Reset Password";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

@using (Html.BeginForm("ResetPassword", "Account", FormMethod.Post))
{
    @Html.AntiForgeryToken()

    <div class="resetPassContent">
        <h4>Şifremi Sıfırla</h4>
        @Html.TextBoxFor(m => m.ResetCode, new { @class = "form-control",placeholder="Onay kodu"})
        @Html.PasswordFor(m => m.NewPassword, new { @class = "form-control",placeholder="Yeni şifre" })
        @Html.PasswordFor(m => m.ConfirmNewPassword, new { @class = "form-control",placeholder="Yeni şifre  (tekrar)" })
        <button type="submit" class="mailActive">Reset Password</button>
    </div>

    <input type="hidden" class="form-control" asp-for="Email" value="@ViewBag.v" />

}
